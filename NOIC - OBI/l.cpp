#include <bits/stdc++.h>
using namespace std;

#define el "\n";
#define _ ios_base::sync_with_stdio(0);cin.tie(0);

int recurs(int blocos, int i){
    
    if(i == 0){
       return(0);
    }

    blocos = (2*i + 1)*(2*i + 1);
    return(blocos);

}

int main(){
    _;

    int blocos, aux, i;

    cin >> i;

    for(int j = 0; j < i; j++){
        aux
    }

    cout << blocos << el;


    return 0;
}